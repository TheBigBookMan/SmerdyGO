//TODO maybe a calendar main with events and then bottom corner a smaller calendar with like dots for events then can click
//TODO could import the data from other sections that show up on the calendar, for example the networking could make meetings with the contacts in events and you clcik on the event and taken to networking for more info on the person.... can do the todo list and tasks are set out on the event calendar as well

const Calendar = () => {
  return (
    <div>
      <h1>Calendar</h1>
    </div>
  );
};

export default Calendar;
