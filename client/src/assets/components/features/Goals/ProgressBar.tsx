const ProgressBar = () => {
  return (
    <div>
      <h1>Progress Bar</h1>

      <p>
        potentially add in like a timeline thing for each subgoal with the title
        and that could probably be the area where the user can change the order
        of them--- would just be an algorithm that changes the index of the
        array
      </p>
    </div>
  );
};

export default ProgressBar;
